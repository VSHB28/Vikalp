@model IEnumerable<Vikalp.Models.DTO.UserDto>
@{
    ViewData["Title"] = "Users";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<style>
    /* .um-action {
        white-space: nowrap;
    }
   
    .um-card { border-radius: 0.5rem; box-shadow: 0 6px 18px rgba(12, 38, 63, 0.08); }
    .um-toolbar { gap: .5rem; display: flex; align-items: center; flex-wrap: wrap; }
    .um-search { max-width: 320px; }

    .um-table thead th {
        color: #fff;
        border: 1px dotted;
        background: var(--ipas-sidebar);
    }
    .um-table tbody tr:hover { background: rgba(13,110,253,0.03); }
    .um-action .btn { min-width: 44px; }
    .um-badge-role { font-size: .8rem; padding: .25rem .45rem; border-radius: .25rem; }
    .um-empty { padding: 2rem; text-align: center; color: #6c757d; }

    .um-action a,
    .um-action button {
        position: relative;
        z-index: 10;
        pointer-events: auto;
    }

    .um-action svg {
        pointer-events: none;
    }

    .table-responsive {
        position: relative;
        z-index: 1;
    }

    .btn-add {
        background-color: #3F0C11;
        color: white;
    } */
</style>

<div class="container py-2">
    <div class="card um-card">
        <div class="card-body">
            <div class="umcard-header">
                <div class="d-flex justify-content-between align-items-start">
                    <div>
                        <h3 class="mb-0">Users</h3>
                        <div class="text-muted small">Manage application users and their roles</div>
                    </div>

                    <div class="um-toolbar">
                        <div class="d-flex um-search">
                            <input id="searchTerm" class="form-control form-control-sm" placeholder="Search by name or email" />
                            <button type="button" class="btn btn-sm  btn-outline-secondary ms-2" onclick="searchUser()">Search</button>
                        </div>


                        <a href="@Url.Action("Create", "User")" class="btn  btn-add btn-primary ms-3" style="z-index: 1000; position: relative;">
                            Add User
                        </a>
                    </div>
                </div>
            </div>
            <hr />

            @if (!Model?.Any() ?? true)
            {
                <div class="um-empty">
                    <p class="mb-1">No users found.</p>
                    <a class="btn btn-outline-primary" asp-action="Create" asp-controller="User">Add your first user</a>
                </div>
            }
            else
            {
                <div class="table-responsive">
                    <table id="usersTable" class="table table-hover um-table align-middle">
                        <thead>
                            <tr>
                                <th style="width:1%;">#</th>
                                <th>Username</th>
                                <th>Email</th>
                                <th>Role</th>
                                <th class="text-center" style="width:8%;">Active</th>
                                <th class="text-end" style="width:18%;">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                        @foreach (var item in Model.Select((value, index) => (value, index)))
                        {
                            var user = item.value;
                            var idx = item.index + 1;
                            <tr>
                                <td>@idx</td>
                                <td>
                                    <div class="fw-semibold">@user.MobileNumber</div>
                                </td>
                                <td>@user.Email</td>
                                <td>
                                    @if (user.RoleName != null)
                                    {
                                        <span class="badge bg-info um-badge-role" title="@user.RoleName">@user.RoleName</span>
                                    }
                                    else
                                    {
                                        <span class="text-muted small">—</span>
                                    }
                                </td>
                                <td class="text-center">
                                    @if (user.IsActive==1)
                                    {
                                        <span class="badge bg-success">Yes</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary">No</span>
                                    }
                                </td>
                                    <td class="text-end um-action">
                                        <a class="btn btn-sm btn-outline-primary me-1"
                                           asp-controller="User"
                                           onclick="goToEdit(@user.UserId)"
                                           asp-route-id="@user.UserId"
                                           title="Edit">
                                            <i class="bi bi-pencil"></i>
                                        </a>
                                        <input type="hidden" asp-for="UserId" />
                                        <form asp-controller="User"
                                              asp-action="Delete"
                                              asp-route-id="@user.UserId"
                                              method="post"
                                              class="d-inline"
                                              onclick="deleteUser(@user.UserId)">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-outline-danger" title="Delete">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </form>
                                    </td>
                            </tr>
                        }
                        </tbody>
                    </table>
                </div>
            }
        </div>
    </div>
</div>

<script>
    function goToEdit(id) {
        window.location.href = '/User/Edit/' + id;
    }


        function deleteUser(userId) {
        if (!confirm("Are you sure you want to delete this user?")) {
            return;
        }

        fetch('/User/Delete', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'RequestVerificationToken':
                    document.querySelector('input[name="__RequestVerificationToken"]').value
            },
            body: JSON.stringify(userId)
        })
        .then(res => res.json())
        .then(data => {
            if (data.success) {
                alert("User deleted successfully");
                window.location.reload();
            } else {
                alert(data.message || "Delete failed");
            }
        })
        .catch(err => {
            console.error(err);
            alert("Error occurred while deleting user");
        });
    }

            async function searchUser() {
        const term = document.getElementById("searchTerm").value;

        const response = await fetch(`/User/Search?q=${encodeURIComponent(term)}`);
        const html = await response.text();

        document.querySelector("#usersTable tbody").innerHTML = html;
    }

</script>
