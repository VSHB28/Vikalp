@using Vikalp.Models
@model List<MenuViewModel>


@if (Model == null || !Model.Any())
{
    <div class="text-muted small">No menu available</div>
}
else
{
    @* <ul class="sidebar-menu list-unstyled">
        @foreach (var menu in Model)
        {
            <li class="menu-item mb-1">
                <a class="d-flex align-items-center text-decoration-none text-white nav-link" href="@menu.Url">
                    @if (!string.IsNullOrWhiteSpace(menu.MenuIcon))
                    {
                        <i class="@menu.MenuIcon me-2"></i>
                    }
                    <span>@menu.MenuName</span>
                </a>

                @if (menu.Children != null && menu.Children.Any())
                {
                    <ul class="submenu list-unstyled ms-3 mt-1">
                        @foreach (var child in menu.Children)
                        {
                            <li class="submenu-item mb-1">
                                <a class="text-decoration-none text-white nav-link" href="@child.Url">
                                    @if (!string.IsNullOrWhiteSpace(child.MenuIcon))
                                    {
                                        <i class="@child.MenuIcon me-2"></i>
                                    }
                                    <span>@child.MenuName</span>
                                </a>
                            </li>
                        }
                    </ul>
                }
            </li>
        }
    </ul> *@
    <ul class="sidebar-menu list-unstyled">
        @foreach (var menu in Model)
        {
            <li class="menu-item mb-1">
                @if (menu.Children != null && menu.Children.Any())
                {
                   
                    <a class="d-flex align-items-center text-decoration-none text-white nav-link menu-toggle" href="javascript:void(0)">
                        @if (!string.IsNullOrWhiteSpace(menu.MenuIcon))
                        {
                            <i class="@menu.MenuIcon me-2"></i>
                        }
                        <span>@menu.MenuName</span>
                        <i class="fas fa-chevron-down ms-auto arrow-icon"></i>
                    </a>

                    <ul class="submenu list-unstyled ms-3 mt-1" style="display: none;">
                        @foreach (var child in menu.Children)
                        {
                            <li class="submenu-item mb-1">
                                <a class="text-decoration-none text-white nav-link" href="@child.Url">
                                    @if (!string.IsNullOrWhiteSpace(child.MenuIcon))
                                    {
                                        <i class="@child.MenuIcon me-2"></i>
                                    }
                                    <span>@child.MenuName</span>
                                </a>
                            </li>
                        }
                    </ul>
                }
                else
                {
                    
                    <a class="d-flex align-items-center text-decoration-none text-white nav-link" href="@menu.Url">
                        @if (!string.IsNullOrWhiteSpace(menu.MenuIcon))
                        {
                            <i class="@menu.MenuIcon me-2"></i>
                        }
                        <span>@menu.MenuName</span>
                    </a>
                }
            </li>
        }
    </ul>
}
<script>
    document.addEventListener('DOMContentLoaded', function() {
        const menuToggles = document.querySelectorAll('.menu-toggle');

        menuToggles.forEach(toggle => {
            toggle.addEventListener('click', function(e) {
                e.preventDefault();

                
                const submenu = this.nextElementSibling;
                const arrow = this.querySelector('.arrow-icon');

                
                if (submenu.style.display === 'none' || submenu.style.display === '') {
                    submenu.style.display = 'block';
                    arrow.classList.remove('fa-chevron-down');
                    arrow.classList.add('fa-chevron-up');
                } else {
                    submenu.style.display = 'none';
                    arrow.classList.remove('fa-chevron-up');
                    arrow.classList.add('fa-chevron-down');
                }
            });
        });
    });
</script>

<style>
    .submenu {
        transition: all 0.3s ease;
    }

    .arrow-icon {
        transition: transform 0.3s ease;
        font-size: 0.8rem;
    }
</style>